# R26 실앱 피드백 프로그램 (44건) — 실행/검증 기록

**시작: 2026-07-20 · 실행 모델: Opus 4.8 · 검증 예정 모델: Fable 5 (2026-07-21~)**

이 문서는 **검증용 단일 기준점**이다. 유저 지시: "이번 질문부터 평가하게 하도록, 나중에 페이블한테 검증받기 쉽게 기록 남겨줘."
따라서 **2026-07-20 유저 피드백 44건(원문 그대로 아래 수록)부터의 모든 작업**이 검증 대상이다. 그 이전 커밋(≤ `7939725` PEN-15 #605)은 검증 대상 밖.

- 프로그램 시작 기준 커밋(baseline): **`7939725`** (master, PEN-15 #605)
- 검증 범위: `git log 7939725..master` 의 모든 커밋 + 이 문서의 각 항목 체크리스트
- 그린 규율(프로젝트 표준): `flutter test > out.txt 2>&1` → **exit 코드($?) 확인 + "All tests passed!" 라인 확인**. `| tail` 금지(exit 코드 가림).
- 성능 판단 기준: **디버그 빌드**. "릴리즈에선 괜찮다"로 마무리 금지.

---

## Fable 5 검증 프로토콜 (내일 이 문서를 먼저 읽을 것)

1. `git log --oneline 7939725..master` 로 이 프로그램의 커밋 전체를 확인한다.
2. 아래 **항목 표**에서 각 항목의 `상태`, `PR`, `구현 요지`, `검증 포인트`를 읽는다.
3. 각 항목마다 다음 3단 검증:
   - **스펙 일치**: 유저 원문(§원문 44건)이 요구한 것과 구현이 일치하는가? 축소/과잉 해석은 없는가?
   - **코드 품질**: 공통 로직화 요구(13/14/34/35 등)가 실제로 공용 유틸로 빠졌는가, 아니면 복붙인가?
   - **테스트**: 계약을 핀하는 테스트가 있는가? 테스트가 실제 회귀를 잡는가(중요: 이 프로젝트는 GPT/AI 작성 테스트가 틀렸던 전례가 있어 전면 재작성 허용됨).
4. 문제 발견 시 **고칠 것**: 유저 위임 범위 안이다. 수정 후 이 문서의 해당 항목에 `Fable 검증:` 줄을 추가하고 무엇을 고쳤는지 적는다.
5. **미착수/부분 구현 항목**은 표의 상태로 정직하게 표기되어 있어야 한다. 상태가 실제와 다르면 그것 자체가 결함이다.

### 라벨 주의 (이름 충돌)
이 프로그램의 코드 주석 마커는 **`R26 #<피드백 번호>`** 다(예: `R26 #26`). 캔버스/선택 코드에 이미 존재하는 **`R26 (C2)` / 무번호 `R26`** 주석은 **다른 세션 라운드**(선택 마스크 후처리 작업)의 라벨이므로 이 프로그램 산출물이 아니다 — baseline `7939725` 이전 커밋에 들어있다. 감사 시 `git log 7939725..master`로 범위를 먼저 자르면 혼동이 없다.

### 이 프로그램에서 지켜야 할 프로젝트 규율(요약)
- PR 바디는 `--body-file`(PowerShell 5.1에서 인라인 큰따옴표 금지), `gh pr create --head <branch>` 명시.
- 생성 플러그인 5파일(`linux/flutter/generated_*`, `macos/Flutter/GeneratedPluginRegistrant.swift`, `windows/flutter/generated_*`)은 **커밋 금지**.
- `Stack` 시블링 수가 변하는 위젯은 **슬롯 키 필수**(과거 3회 재발 사고).
- `==` 오버라이드 타입(LayerId 등)은 `const` 컬렉션 금지.
- 레일 행/레전드 메모 토큰(`_RailRowMemoInputs`/`_LegendMemoInputs`)에 새 표시 팩트 추가 시 **토큰 동참 필수**(누락=토글 스테일).
- `RawGestureDetector` 인식기가 스크롤과 경쟁하면 **`gestureSettings` 수동 주입 필수**(GestureDetector와 달리 자동 주입 없음).
- 파괴적 git 명령은 `-C <절대경로>` 명시.

---

## 항목 표 (44건)

상태 값: `TODO` / `WIP` / `DONE` / `DEFERRED(사유)` / `ANSWERED(질문 항목)`

| # | 요약 | 클러스터 | 상태 | PR |
|---|------|---------|------|----|
| 1 | 여러 행 범위 프레임 생성 = undo 1단계 | A 타임라인 코어 | DONE | #606 |
| 2 | 멀티 행 이동 시 SE→다른 SE 블록 이동 불가 | A | DONE | #606 |
| 3 | 트랜스폼/fx 행 = 프레임 셀과 완전 동일한 선택 도메인(액티브 레이어 불변, 화살표 내비는 건너뜀) | B 선택 도메인 | TODO | |
| 4 | 타임시트 줌 35% 미만에서 텍스트 소멸 | C 비주얼 | DONE | #606 |
| 5 | 터치 드로잉 모드에서 타임시트 패널 2핑거 조작이 선을 그림 | D 입력 | DONE | #607 |
| 6 | 1핑거 플립이 타임시트 패널에서 어떻게 동작하나(막고 있나? 페이지 전환?) | D | ANSWERED | — |
| 7 | 프레임 블록에 콤마/초수 표시(스토리보드 컷 블록 로직 재사용, 블록 끝 칸 우하단) | C | TODO | |
| 8 | 컷 블록 엣지 색 = 배경 밝기별 2종 + 호버색 추가 | C | TODO | |
| 9 | 브러시 자체 블렌딩 모드(컬러/배경에 묘화/삭제 + 레이어 공통 모드) | E 툴 | TODO | |
| 10 | 브러시 사이즈·블렌딩 모드는 브러시 선택과 독립 | E | TODO | |
| 11 | 브러시 설정: 컬러/팁셰이프 삭제 + 클튜식 세팅별 필압 커브 | E | TODO | |
| 12 | 선택툴 세팅에서 사각/올가미 선택 항목 삭제(툴 라이브러리가 제공) | E | DONE | #607 |
| 13 | 변형툴: 선택 없어도 전체 그림 이동 / 대상 없으면 안내문구+툴 전환 무시(공통 안내 시스템) | E | 부분 DONE | #608 |
| 14 | 변형툴 x/y/각도/스케일 = 캔버스 하단 드래그 숫자 위젯 공통 로직 재사용 | E | TODO | |
| 15 | 선택툴: 프레임 없어도 범위 선택 가능 | E | TODO | |
| 16 | 선택툴 모드 4종(갱신/추가/삭제/선택중), 기본=추가 | E | TODO | |
| 17 | Ctrl+T = 변형툴로 전환(별도 변형 동작 아님) | E | DONE | #607 |
| 18 | 선택 상태에서 다른 툴 사용 가능(선택 내부만 그리기/칠하기) | E | TODO | |
| 19 | 와콤 펜 아래 버튼(우클릭)에 스포이드 할당해도 미작동 | D | 부분 DONE | #607 |
| 20 | 우클릭 스포이드 = 툴을 스포이드로 전환해 실행(UI 동일) | D/E | 부분 DONE | #607 |
| 21 | 스포이드 사용 중 커서 우상단 실시간 색 표시 ON | E | 기존 구현(원인=19·20) | #607 |
| 22 | 스포이드 커서 = 스포이드 아이콘 | E | DONE | #608 |
| 23 | 필 툴 커서 = 바케츠 아이콘 | E | DONE | #608 |
| 24 | 상단 메뉴의 "Quick Animaker" 삭제 | G 셸 | DONE | #606 |
| 25 | 전체창/창모드 옵션 + 전체화면 메리트 질문 | G | 미착수 답변만 | — |
| 26 | 실 데이터 표기모드가 홀드 셀까지 실데이터로 표기 | A | DONE | #606 |
| 27 | 블록 성질/코마 변경 시 텍스트가 순간 연해졌다 진해짐 | C | TODO | |
| 28 | 레이어 타입 아이콘/fx 트월 호버 실루엣 = 원형으로 | C | DONE | #606 |
| 29 | 어태치 레이어 이름 = 원본이름+1 (예 `B+1`) | F 레이어 | DONE | #606 |
| 30 | 레이어에 블렌딩 모드 버튼 추가 | F | TODO | |
| 31 | 기본 독 배치: 타임시트=오른쪽 세로 넓은 독, 왼쪽 위=툴 라이브러리, 왼쪽 아래=툴 세팅 | G | DEFERRED(테스트 코퍼스 라운드 필요) | #609 |
| 32 | 타임라인에 프로젝트 FPS 설정 버튼(프리셋 8/12/23.976/24…) | G | DONE(정수 프리셋) | #609 |
| 33 | 인풋 인스펙터 정식 기능화 + 열고닫을 때 레이아웃 점프 제거 | D | TODO | |
| 34 | 팬 숏컷(2핑거/휠클릭)이 타임라인에서도 팬 — 공통 로직화, none이면 팬 없음 | D | TODO | |
| 35 | 프레임 없는 곳/액션섹션 아닌 곳에서 그리기 시도 = 커서 옆 1초 안내(공통 로직) | E | DONE | #608 |
| 36 | 어태치 레이어는 항상 주인 레이어에 인접(사이 삽입 금지) + 트랜스폼 설정 아래 배치 | F | 부분 DONE | #606 |
| 37 | 더블탭 이름변경 = 같은 인덱스 연속 탭일 때만 | A | DONE | #606 |
| 38 | 타임라인 줌 50% 이하에서 블록 텍스트/룰러 텍스트 소멸 금지 | C | DONE | #606 |
| 39 | 룰러 텍스트가 극단 줌아웃에서 사라지는 문제 | C | DONE | #606 |
| 40 | 룰러도 프레임 그리드와 통일(6f 약간 진한 선, 24f 진한 선) | C | DONE | #606 |
| 41 | 타임시트 페이지뷰 = 한 번에 한 장 + 하단 페이지 내비(표기전환/페이지뷰전환/◀/페이지/▶/스크롤바) | G | TODO | |
| 42 | 캔버스 하단 아이콘 스타일을 기본 아이콘 UI로 책정, 타임시트 패널부터 통일 | C | TODO | |
| 43 | 종료 시 편집(undo 스택 기준) 있을 때만 저장 프롬프트(예/취소/저장/다른이름) | G | DONE | #609 |
| 44 | 액션섹션 한정, 그림 없는 블록은 바탕색으로 구분 | C | TODO | |

---

## 클러스터별 상세 (구현 요지 / 검증 포인트)

> 각 항목은 구현할 때 이 절에 **구현 요지 + 검증 포인트 + 테스트 파일**을 채운다.
> 비어 있는 항목 = 아직 미착수(상태 표와 일치해야 함).

### A. 타임라인 코어 (1, 2, 26, 37)

**#1 범위 생성 = 1언두 (DONE, PR #606)**
- 구현: `EditorSessionManager.createInstancesForSelection`이 카메라/지시/드로잉 3경로의 커맨드를 **수집**해 하나의 `CompositeCommand`로 실행. 카메라 커맨드가 **반드시 첫 번째**(그 undo가 프로젝트 스냅샷 복원 = 역순 undo의 마지막이어야 전체가 되돌아감). `TimelineController.drawingFramesCommandsForLayers`를 신설해 커맨드만 반환(기존 `createDrawingFramesForLayers`는 그 위의 래퍼).
- 검증 포인트: 카메라+지시+드로잉을 모두 덮는 범위 선택 → Add → **undo 1회**로 전부 소멸. 파일: [create_for_selection_test.dart](test/ui/create_for_selection_test.dart) 'R26 #1'.
- 주의(감사용): 커맨드 순서가 바뀌면 undo가 부분 복원된다. 이 순서 계약이 코드 주석에 명시돼 있는지 확인할 것.

**#2 멀티 행 이동 시 SE 행 (DONE, PR #606)**
- 원인: `_updateMultiRowRangeMove`가 드로잉 격자(lattice)만 알아서, 범위에 내용 있는 SE 행이 끼면 **전체를 프레임 슬라이드로 강등**시켰다.
- 구현: SE 행을 리지드 이동의 **승객(passenger)** 으로 승격 — `_planMultiRowSePassengers`가 동일 rowDelta를 SE 격자(`activeTrack.seLayers`)에 적용, `planSeRangeRowMove` 재사용(오디오 클립은 셀에 앵커돼 자동 추종). 하나라도 못 내려앉으면 전체 무효(멀티행 all-or-nothing) → 마지막 유효 프리뷰 유지(R23 #10). 커밋=글로벌 폼 `UpdateLayerTimelineCommand` 쌍이 같은 합성 언두에 합류. 앵커가 SE 행이어도 rowDelta 계산됨(`rowDeltaWithin` 2격자 폴백).
- 검증 포인트: [editor_session_manager_range_move_test.dart](test/ui/editor_session_manager_range_move_test.dart) 'R26 #2'.

**#26 실 데이터 시트 = 홀드는 한 셀 (DONE, PR #606)**
- 원인: 데이터 모드가 **모든 고스트를 verbatim**으로 인쇄 → 홀드 고스트마다 같은 셀 라벨이 재출력(앞홀드+뒤홀드면 3개).
- 구현: [timesheet_document.dart](lib/src/models/timesheet_document.dart) — 데이터 모드에서 **리피트 고스트만** verbatim, **홀드 고스트는 노테이션 경로**로 유지. 뒤홀드는 데이터 모드 전용 분기로 소유 런의 drawing 셀 span을 늘리고 나머지 행은 `held`. 앞홀드는 기존 relocation(첫 행에 셀) 재사용.
- 검증 포인트: [timesheet_data_sheet_test.dart](test/models/timesheet_data_sheet_test.dart) 'R26 #26' 2건 — 뒤홀드/앞+뒤홀드 모두 **drawing 셀은 정확히 1개**.
- 유저 질문 "토메 표기도 간단해지나": 노테이션(止め) 경로는 손대지 않았다. 데이터 모드만 바뀜.

**#37 더블탭 이름변경 = 같은 인덱스 (DONE, PR #606)**
- 원인: Flutter `DoubleTapGestureRecognizer`는 2번째 탭이 **100px 슬롭** 안이면 같은 더블탭으로 받는다 → 블록 안 다른 셀 연속 탭이 이름변경.
- 구현: [timeline_cell_double_tap.dart](lib/src/ui/timeline/timeline_cell_double_tap.dart) `TimelineCellDoubleTapGate` — 행의 raw `Listener`가 탭다운 셀을 기록하고, `onDoubleTapDown`이 같은 셀일 때만 활성화. **시간 검사 없음**(인식기가 이미 300ms 창을 강제; 월클럭 검사는 풀 스위트 부하에서 플레이키 — 실제로 1차 풀런에서 이 사고가 났고 그래서 제거함).
- 이벤트 순서 계약: 자식(GestureDetector)이 조상(Listener)보다 먼저 이벤트를 받으므로, 두 번째 탭다운 시점의 기록은 **첫 탭의 것**이다. 이 계약을 [cell_double_tap_same_index_test.dart](test/ui/timeline/cell_double_tap_same_index_test.dart)의 "다른 셀" 케이스가 핀한다(순서가 반대였다면 통과 못 함).

### B. 선택 도메인 (3)

미착수.

### C. 비주얼 (4, 7, 8, 27, 28, 38, 39, 40, 42, 44)

**#38 + #4 텍스트 소멸 → 축소 (DONE, PR #606)**
- 원인: `timeline_row_cells_painter.dart`가 `frameCellExtent < 14`에서 glyph를 `''`로 비웠다(타임라인/x시트 공통 경로라 35% 축소 시 시트 텍스트도 같이 사라짐).
- 구현: 그 가드 삭제 + [timeline_cell_style.dart](lib/src/ui/timeline/timeline_cell_style.dart) `timelineFittedGlyphFontSize(base, extent)` — 14px 미만이면 `extent*0.78`, 하한 4.0. `glyphStyleFor`가 이 크기를 쓰므로 **타일 베이크 키에도 자동 반영**(폰트 크기가 캐시 키 구성요소).
- 룰러 라벨(매 프레임 라벨 모드)도 같은 함수 적용.

**#39 룰러 텍스트 잘림 (DONE, PR #606)** — 원인: 룰러가 셀마다 `배경→라벨`을 한 루프에서 그려서, 좁은 칸의 라벨이 **다음 칸 배경에 덮여** 반쯤 사라졌다. 구현: [timeline_frame_ruler_painter.dart](lib/src/ui/timeline/timeline_frame_ruler_painter.dart) 3패스(배경 → 그리드선 → 라벨)로 분리.

**#40 룰러 그리드 통일 (DONE, PR #606)** — [timeline_beat_lines.dart](lib/src/ui/timeline/timeline_beat_lines.dart)에 `timelineFrameBoundaryLineInk()` 공용 함수(베이스 케이던스/6f/초 경계) 신설, 룰러 2패스가 이걸 사용. 셀 그리드와 같은 색·굵기.

**#28 아이콘 호버 원형 (DONE, PR #606)** — 레이어 타입 버튼/레인 트월/어태치 트월 InkWell에 `customBorder: CircleBorder()` (타임라인+x시트).

**#7 / #8 / #27 / #42 / #44**: 미착수.

### D. 입력 (5, 6, 19, 20, 33, 34)

**#5 타임시트 2핑거가 선을 그림 (DONE, PR #607)**
- 원인: 멀티터치 방어가 **뷰 인스턴스 로컬**이었는데, 타임시트는 시트 창마다 `InteractiveBrushEditCanvasView`를 하나씩 마운트한다 → 두 손가락이 서로 다른 뷰에 내려앉으면 각 뷰는 "손가락 1개"만 보고 둘 다 그린다.
- 구현: [canvas_touch_contacts.dart](lib/src/ui/canvas/canvas_touch_contacts.dart) `CanvasTouchContacts` — 앱 전역 손가락 센서스 + **멀티터치 리스너**. 두 번째 접촉이 어디든 내려오면 이미 진행 중이던 (미커밋) 터치 스트로크가 형제 뷰에서도 스탠드다운. dispose 시 자기 접촉 정리(누수=영구 차단 방지).
- 검증 포인트: [shared_touch_census_test.dart](test/ui/canvas/shared_touch_census_test.dart) — **이 테스트는 픽스를 되돌리면 실제로 실패함을 확인했다**(되돌린 상태 = 커밋 2건, 픽스 = 0건). 테스트 전제: 1핑거 슬롯을 `draw`로 세팅해야 의미가 있음(안 그러면 애초에 안 그려져 공허 통과 — 최초 작성 시 실제로 그 함정에 빠졌다가 잡음).

**#6 (답변)**: 1핑거 플립은 **캔버스 뷰포트 제스처 레이어에만** 배선돼 있다(`canvas_viewport_gesture_layer.dart`). 타임시트 패널은 그 위젯 트리 밖이라 구조적으로 플립이 발동하지 않는다 = "제대로 막고 있다"가 맞다. 좌우 플립=페이지 전환 아이디어는 #41(페이지뷰) 작업에서 함께 검토 예정(미구현).

**#19 + #20 (부분 DONE, PR #607)**
- 원인 진단: 와콤 배럴 버튼은 **호버 중** 우클릭 비트로 도착하는데, 캔버스 매핑(`CanvasPointerAction.eyedropper`)은 `_handlePointerDown`에서만 처리됐고 호버 경로는 undo/redo만 처리하고 스포이드는 `break`(주석: "hold-tools need a contact")였다 → 눌러도 아무 일도 안 일어나고 UI도 안 뜸.
- 구현: 호버 프레스 엣지에서 `onTemporaryToolHold(CanvasTool.eyedropper)` 발화 = **툴 자체가 스포이드로 전환**(유저 #20 설계) → 커서/스와치/패널이 따라옴. 릴리즈 엣지에서 스프링백(또는 keep). 접촉이 생기면 기존 매핑 다운 경로가 인계(홀드 세션 1개 유지).
- **실기 검증 필요**: 와콤 드라이버가 호버 배럴 프레스를 보고하지 않는 구성도 있다(Windows Ink 설정). 실기에서 Input Inspector로 확인 요망.

**#33 / #34**: 미착수.

### E. 툴 (9~18, 21, 22, 23, 35)

**#35 + #13 공통 안내(커서 노티스) 시스템 (DONE=#35 전체·#13 툴 가드, PR #608)**
- 신설: [cursor_notice.dart](lib/src/ui/widgets/cursor_notice.dart) — `CursorNoticeController`(메시지 1개+revision+타이머)·앱 전역 `cursorNotices`·`CursorNoticeOverlay`(셸 전체를 감싸며 포인터를 수동 추적, 커서 옆 알약으로 ~1초 표시). **UI 교체 시 오버레이만 손대면 됨**(유저 요구: "ui 바껴도 다른데도 적용가능하게"). 문구는 [app_strings.dart](lib/src/ui/text/app_strings.dart)에 5개 언어 추가.
- 사용처 1(#35): `MainCanvasBrushHost.onDrawRefused` — 편집 가능한 셀이 없을 때 페인트 툴로 누르면 셸이 **섹션 판정**해서 "프레임이 존재하지 않습니다"(드로잉 섹션) 또는 "액션 섹션에서만 그릴 수 있습니다"(그 외)를 띄움. Listener는 translucent라 뷰포트 조작을 먹지 않음.
- 사용처 2(#13 전반): **툴 전환 가드 퍼널** — `PaintToolStateNotifier.switchGuard`/`onSwitchRefused`. 모든 툴 진입점(라이브러리 패널·단축키·Ctrl+T·펜 버튼)이 이 세터 하나를 통과하므로 우회 불가. HomePage가 `move` 전환 시 `celHasRenderableContent`로 그림 유무 확인 → 없으면 안내+전환 무시.
- **#13 잔여**: "선택이 없어도 그림 전체를 변형 대상으로" 자체는 미구현(현재는 그림이 있으면 툴 전환은 되고, 변형 박스가 선택 없이 전체를 잡는 동작은 CanvasSelectionLayer 쪽 후속 작업).
- 검증 포인트: [cursor_notice_test.dart](test/ui/cursor_notice_test.dart) — 오버레이 표시/자동 소멸, 가드가 툴만 거부하고 같은 write의 설정은 통과시키는 계약. **함정 기록**: 전역 컨트롤러의 타이머가 위젯 트리보다 오래 살아 "Timer still pending"으로 무관한 테스트를 깨뜨렸다 → 오버레이 dispose에서 `clear()`.

**#22 스포이드 커서 / #23 필 바케츠 커서 (DONE, PR #608)** — 시스템 커서를 `none`으로 감추고 툴 아이콘(`Icons.colorize` / `Icons.format_color_fill`)을 포인터에 그림(`_ToolCursorIcon`: 흰 글리프+어두운 헤일로). 필은 자체 호버 추적 노티파이어(`_toolCursorHover`) 사용. **#21은 이미 충족**(스포이드 툴 활성 시 커서 우상단 실시간 스와치가 기존에 있었음 — 안 뜨던 원인이 #19/#20이었고 그건 batch2에서 해결).

**#12 선택 변형(사각/올가미) 설정 삭제 (DONE, PR #607)** — 툴 세팅의 `selection-variant-segments` 제거. 마스크 노브(grow/feather/AA)는 유지.

**#17 Ctrl+T = 변형툴 전환 (DONE, PR #607)** — `selection-free-transform` 액션이 `beginTransform()` 직접 호출 대신 `CanvasTool.move`로 툴을 바꾼다.

**#9~#11, #13~#16, #18, #21~#23, #35**: 미착수. (#13/#35의 공통 안내 UI가 다음 배치 1순위)

### F. 레이어 (29, 30, 36)

**#29 어태치 이름 = 베이스+부호 (DONE, PR #606)** — `nextAttachedLayerName`이 `'${base.name}$sign${n}'` 반환(`B+1`, `B-1`). 기존 테스트 3곳 기대값 갱신.

**#36 어태치 그룹 불가분 (부분 DONE, PR #606)** — 베이스가 액티브일 때 새 레이어가 **그룹 끝 뒤**에 삽입되도록(기존엔 베이스 바로 위 = 그룹 내부 침입). **잔여**: "트랜스폼 설정 열면 어태치 레이어 밑에 오도록"(행 순서 = 베이스 → 어태치 → 트랜스폼 레인)은 미구현 — 표시 순서 방향(리버스 여부)을 실앱에서 확인해야 안전해서 보류. Fable 검증 시 여기 우선.

**#30**: 미착수.

### G. 셸/프로젝트 (24, 25, 31, 32, 41, 43)

**#24 상단 앱 이름 삭제 (DONE, PR #606)** — home_page 상단 스트립의 'QuickAnimaker' 텍스트 제거, widget_test 기대값 `findsNothing`으로 갱신.

**#43 종료 프롬프트 (DONE, PR #609)**
- 편집 여부 = `session.hasUnsavedChanges`(히스토리 매니저가 커맨드 실행마다 세우는 더티 플래그 = undo 스택 기준). **깨끗하면 다이얼로그 없이 즉시 종료**.
- 버튼 4개: Cancel / Save / Save As… / Close. Save·Save As는 **기존 File 메뉴 경로 재사용**(`promptSaveProjectAs`·`saveProjectToFile`) — 저장 실패나 피커 취소면 여전히 더티라 종료를 취소한다(`return !hasUnsavedChanges`).
- 검증 포인트: [system_exit_gate_test.dart](test/ui/system_exit_gate_test.dart) — 편집됨=4버튼 다이얼로그+Cancel 생존 / 미편집=다이얼로그 없음.

**#32 프로젝트 FPS (DONE=정수 프리셋, PR #609)**
- 모델: `Project.fps`(기존 int) + 신규 `UpdateProjectFpsCommand`·`repository.updateProjectFps` → `session.setProjectFps`(1언두, 같은 값/0 이하=노옵).
- UI: 타임라인 툴바에 `24 fps` 플라이아웃 버튼(프리셋 8/12/15/24/25/30/48/50/60 + Custom… 다이얼로그).
- **정직한 한계**: 유저가 언급한 **23.976/29.97(NTSC 풀다운) 미지원** — `fps`가 int이고 모든 타이밍 소비자(룰러 초 경계·시트 행·오디오 프레임 변환·재생)가 정수 프레임 연산이라, 분수 레이트는 int→double(또는 rational) 마이그레이션이 선행돼야 한다. 이번 배치에 몰래 끼워넣지 않았다. **다음 라운드 후보**.
- 검증 포인트: [project_fps_test.dart](test/ui/project_fps_test.dart).

**#31 기본 독 배치 (DEFERRED — 시도했고 되돌림, PR #609에 사유 기록)**
- 시도한 배치: 오른쪽 독=타임시트 / 왼쪽 넓은 독=툴 라이브러리(위)+툴 세팅(아래) / 하단=타임라인·스토리보드.
- 결과: **풀 스위트 201건 실패**. 두 가지 근본 원인 ①툴 패널 Column이 반높이 독에서 오버플로(→ 이건 고쳐서 유지: `SingleChildScrollView`) ②**타임시트가 기본으로 보이게 되면서** 위젯 파인더가 중복 매치(`Bad state: Too many elements`) — 시트가 자기 잉크 뷰/셀을 마운트하기 때문.
- 판단: 기본 배치 변경은 **테스트 코퍼스 전면 정비를 동반하는 자체 라운드**가 필요하다(파인더를 패널 스코프로 좁히는 작업). 반쯤 고쳐 넣는 것보다 미착수로 남기는 게 정직하다고 판단해 되돌렸다. 유지된 부산물: 툴 패널 스크롤(#31 준비 + 좁은 독에서의 실제 버그 수정).

**#25 / #41**: 미착수.

---

## §원문 44건 (유저 피드백, 2026-07-20 — 손대지 말 것)

1. cam레이어,지시레이어,se레이어등 여러 행 선택범위 프레임 생성시 undo하면 한번에 모든 만들었던 프레임 지우도록. 지금은 지시레이어랑 cam레이어랑 se,애니레이어로 3단계로 나눠서 지워짐. 1단계로 다 지우도록
2. 아직도 행 여러개 선택하고 이동시킬때, se레이어가 다른 se레이어블록으로 이동안됨.
3. 트랜스폼쪽, 트랜스폼 행 자체도 선택가능하게하고 애초에 지금 fx는 한 행만 뭔가 독립적인 사각형 ui로 선택발동하는데, 다른 프레임셀이랑 완벽동일시켜. 여러행 선택가능하고. 대신 액티브 레이어가 바뀌지는 않고. 여기서 추가 제안으로서 내부 fx행(트랜스폼행이나 트랜스폼의 내부 행) 모두에 적용되는 그 행을 선택할수있는 기능 제공. 선택한다고해서 액티브레이어는 바뀌지않음. 다른 레이어의 fx행쪽 선택하면 액티브 레이어는 바뀜. 즉 상태가 액티브레이어 선택/추가로 fx행 선택 이렇게 두개 존재하는느낌임. 다만 화살표키로 레이어 선택 바꾸는건 fx행은 레이어는 아니기때문에 건너뜀.
4. 타임시트, 줌 35%미만으로 축소하면 텍스트들이 사라짐
5. 터치 드로잉모드시 캔버스는 괜찮은데 타임시트패널에서 2핑거할때나 2핑거 이상으로 조작할때 선이 그려져버림
6. 1핑거 플립일때, 타임시트패널에서 플립은 어떤식으로 작동하지? 플립 할것도 없으니까 제대로 막고있는거맞나? 좌우플립은 페이지 전환이라던가? 페이지뷰일때.
7. 프레임 블록에 추가기능으로 블록의 콤마/초수 보여주는 기능. 콤마/초수 설정에 따라 다른거 보여주도록. 스토리보드패널의 컷블록의 초수 보여주는거랑 동일한 로직사용. 위치도 블록의 가장 끝 칸의 오른쪽밑정렬 x시트도 동일하게 가장 끝 칸의 오른쪽밑정렬
8. 컷 블록의 엣지, 바탕색이 검정이라 엣지도 회색쪽이라 너무 눈에안띔. 강조색은 지금그대로 괜찮은데 기본색을 뒤 배경이 어두울때/밝을때 두가지로 나눠서 설정. 추가로 호버색도 추가하도록.
9. 브러시 설정, 브러시 자체 블렌딩모드 도입. 가장 필요로 생각하는건 컬러(일반 상태, 기본값), 배경에 묘화(현재 그림의 뒤에 그리기), 삭제(지우개도 있지만 블렌딩모드로도 제공), 그 외 레이어랑 공통적인 블렌딩모드들
10. 브러시 설정의 사이즈,블렌딩모드는 브러시 선택이랑 독립적으로 작동. 즉 브러시 다른거 선택한다고 브러시사이즈/블렌딩모드가 바뀌지않음. 이걸 베이스로 브러시를 조정해야하면 물론 조정 자유. 아직 릴리스도 안했기때문
11. 브러시 설정에 있는 컬러 삭제. 컬러휠패널이 생겼으니. 팁 셰이프도 삭제해도 되지않나? 그리고 클튜 브러시 설정 어떻지? 사이즈/오파시티/하드니스 등 브러시 세팅별로 필압설정 가능하지않나? 그거 도입. 위치는 사이즈행의 옆에 공간만들어서 버튼 추가? 클튜참고해서 디자인 제안해줘.
12. 선택툴, 사각인지 올가미인지는 툴 라이브러리에서 제공하니까 툴 세팅에서 선택하는건 삭제
13. 변형툴, 선택하지 않은 상황이어도 변형툴 이동 툴 시작가능하게. 기준은 내부 그림 전체를 이동. 다만 물론 그림이 없으면 변형대상이 없다고 안내문구 띄우고 변형툴로 변경을 무시함(이 안내문구 시스템 다른데서도 사용하게 공통로직화. ui 바껴도 다른데도 적용가능하게). 선택후 내부에 그림이 없을 때 변형툴 선택할때도 변형툴 변경 무시. 즉 변형툴 변경할때 변경할 그림 없으면 안내문구+변경 무시
14. 변형툴, x,y,각도,스케일 조절은 캔버스패널에 존재하는 표시숫자 드래그 조절버튼(줌이나 각도 조절하는거) 로직을 공통로직으로서 재사용.
15. 선택툴, 프레임이 존재 안하더라도 일단은 어느 상황에서든 무조건 선택으로 범위선택은 할수있도록
16. 선택툴, 클튜처럼 선택마다의 작동방식을 갱신/추가/삭제/선택중을 선택 4가지 기능구현. 기본값은 추가
17. 컨트롤 t 동작은 새로운 변형동작이 아니라 툴을 변형툴로 바꾸도록. 그래야 로직이 공통화되고 깔끔해지지.
18. 선택툴로 선택하고 다른 툴 변경가능. 어떤 툴이던 변경가능. 선택하고 그리면 선택내부만 그려진다던가 선택내부만 칠한다던가. 클튜같은 다양한 상황에서 활용할 용도
19. 와콤 스타일러스펜의 밑 버튼은 기본적으로 윈도우에서 우클릭판정인데, 우클릭에 스포이드 할당해도 스포이드 작동안함.
20. 우클릭 스포이드가 실제 스포이드 툴이랑 로직이다름. 우클릭 스포이드기능은 툴을 스포이드로 바꾸고 우클릭하는동안 스포이드 실행하도록. 지금 ui도 안뜨는데 그것의 근본 해결안이라고 생각함
21. 스포이드툴, 사용중인지 아닌지 알기어려우니까 사용중에만 실시간으로 색 커서 오른쪽위에 비치는거 on하도록
22. 스포이드 툴 커서 십자가가 아니라 스포이드 툴 아이콘 그대로사용
23. 필 툴은 커서 바케츠 아이콘 사용
24. 상단메뉴에 있는 퀵 애니메이커 삭제
25. 전체창모드/창모드 기능 옵션 추가. 전체화면모드는 메리트 있을까? 뭐 빨라지거나 하나? 렌더링?
26. 타임시트 실 데이터표기모드, 이거 실 데이터 표기하는거맞아? 인덱스1에 그림1을두고 홀드하면 인덱스1 에만 프레임있고 인덱스2부터 비어있어야하는거아닌가? 왜 인덱스2에도 그림1이 나와서 그림1이 두개나 존재하지? 원인파악해서 해결. 좀 더 테스트하니 홀드성질마다 프레임 넣어서 중간에 있는 그림1을 앞홀드 뒤홀드하면 실 데이터모드의 타임시트가 1번인덱스에 그림1, 중간에 그림1, 바로다음 그림1 이렇게 세개나 존재하는데 그게 아니라 이 경우 실 데이터는 1번인덱스에만 존재하게해야지. 이거로 토메표기도 더 간단해지려나?
27. 프레임블록 성질 바꿀때마다 다른 모든 레이어의 타임라인에있는 텍스트가 순간적으로 연해졌다 진해짐. 그리고 프레임블록 코마 조절하면 현재 레이어의 텍스트들이 연해졌다 진해짐. 이런 순간적으로 연해졌다 진해지는문제 근본적으로 해결
28. 레이어의 타입아이콘, fx접기펼치기 아이콘이 호버시 네모난 실루엣의 호버표시인데, 다른거랑 동일하게 동그랗게
29. 어태치레이어의 이름짓기타입, 지금 그냥 +1 +2 이런식인데 원래 레이어이름 뒤에 +1 붙이도록. 예를들면 원래가 B면 B+1, 중간에 띄어쓰기 없이.
30. 레이어에 블렌딩모드 버튼 추가
31. 타임시트패널 기본 위치 오른쪽 세로 넓은독, 툴 라이브러리패널은 지금처럼 왼쪽 세로 넓은독의 위, 그 아래를 툴 셋팅패널
32. 타임라인에 프로젝트의 fps 설정하는 버튼 추가. fps는 프로젝트 내에서 통일. 프리미어프로나 다른 프로 프로그램 참고해서 fps 프리셋 제공(8,12,23.97,24 등). 앞으로 이 fps가 프로젝트의 기본 축이 될것임
33. 인풋 인스펙터는 릴리즈할때도 사용자가 테스트할수있게 정식기능으로 도입할것임. 그러니 인스펙터 열고닫을때 화면 레이아웃이 순간 바꼈다가 돌아오는문제 해결
34. 지금 2핑거드래그가 타임라인에서 팬으로 작동하고있는데, 이거 맘에 듬. 대신 지금 2핑거드래그를 none으로해도 팬이 작동하니까 좀 더 로직을 발전시켜서 팬이 발동하는 숏컷(현 기본값인 2핑거 드래그나 휠클릭)의 경우, 타임라인에서는 팬으로 작동하도록 로직공통화
35. 공통로직 생성, 액션섹션이지만 프레임이 없는 곳에서 브러시/지우개/필 등으로 그리려하면 커서쪽에 1초정도 프레임이 존재하지 않습니다 라고 표시. 기본 커서 따라다님. 액션섹션이 아닌곳에서는 그리는게 불가능하니 그 경우 액션섹션에서만 그릴 수 있습니다라고 표시
36. 어태치 레이어는 항상 주인레이어에 붙어있음. 지금 주인레이어에서 레이어생성하면 주인레이어/새 레이어/어태치레이어들 이렇게 되버리는데, 트랜스폼 설정마냥 주인레이어에 항상 붙어있도록, 그 사이 접근을 허용하지않음. 그리고 어태치레이어가 제일 첫번째 자식이라는 룰에 따라서 트랜스폼 설정 열면 어태치레이어 밑에오도록 바꾸자.
37. 더블탭으로 프레임 이름 변경하는거, 같은 인덱스만 클릭했을때 변형작동하도록. 지금은 블록 내에서 다른인덱스 연속으로 탭하면 이름변경ui뜸
38. 프레임블록 이름,마크 등 텍스트가 타임라인 줌 50% 정도대로 떨어지면 텍스트가 사라짐. 엄청 작아지는 한이있어도 절대 안사라지도록. 룰러쪽 텍스트도 어느정도 지나면 사라지는데 안사라지도록
39. 타임라인 룰러 텍스트, 아직 심하게 줌 아웃하면 텍스트 절반이 다음 칸이 위에 올라와서그런건지 모르겠는데 사라짐.
40. 타임라인 룰러도 프레임 셀 그리드랑 통일감내기. (6f마다 살짝진한선,24f마다 진한선 그대로사용.)
41. 타임시트 패널, 페이지뷰이고 페이지가 여러개일때 용지 여러장이 생기는데, 그게 아니라 페이지 넘기는 조작 가능하도록 해서 한번에 한 용지만 표시. 하단 패널프레임의 스크롤바 왼쪽에 페이지뷰전환버튼 넣고 그 오른쪽에 왼쪽화살표아이콘(이전페이지), 페이지 아이콘(시트의 シート랑 동일하게 1/2 2/2 이런식으로 표기, 여기에 공통 텍스트드래그로직 적용해서 드래그로 페이지이동가능), 오른쪽화살표아이콘(다음페이지)/ 그 다음이 가로스크롤바가 되도록 이렇게 버튼추가. 추가로 실 데이터표기모드버튼도 그쪽으로 옮겨서 최종적으로 표기전환/페이지뷰전환/왼쪽/페이지/오른쪽/스크롤바 이렇게 되도록.
42. 캔버스패널 하단의 아이콘 스타일 아주 맘에들거든? 이걸 앞으로 기본 아이콘ui스타일로 책정. 다른곳 통일. 우선 타임시트패널에 있는것들 이 느낌대로 통일(위에있는 드로잉허가버튼들이나 타임라인쪽 버튼들)
43. 프로그램 닫을 때, 편집된 것이 있어야만 창 띄우고 내용은 저장하지 않았습니다. 닫겠습니까? 이런식으로. 버튼으로 예/캔슬/저장/다른이름으로저장 이렇게 제공. 로직은 각각 알아서 원래 있던거 연결. 편집의 기준은 undo스택기준
44. 액션섹션 한정, 그려진 그림이 존재하지 않으면 알 수있게하는 ui 제공. 블록의 바탕색을 기존보다 살짝 회색으로?
