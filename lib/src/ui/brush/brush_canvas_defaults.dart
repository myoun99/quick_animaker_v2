import '../../models/canvas_size.dart';

/// Production defaults for embedded Brush canvas surfaces.
class BrushCanvasDefaults {
  const BrushCanvasDefaults._();

  static const canvasSize = CanvasSize(width: 2340, height: 1654);
}
